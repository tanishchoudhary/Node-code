# This workflow will do a clean install of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-wi
name: Node.js CI

on:
  push:
    branches: main 
  

jobs:
  build:

    runs-on: ubuntu-latest

    
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
    - run: echo "hello"
    - run: |
        curl \
        -X POST \
        -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/tanishchoudhary/Node-code/actions/workflows/t.yml/dispatches \
        -d '{"ref":"main"}'

  call-workflow-passing-data:
    uses: ./test/.github/workflows/test.yml
